class Cat: public Animal {
  private:
  unsigned int legs;
  public:
  int color;

  Cat(int count_legs = 4): legs(count_legs) {}

  void meow() {
    int meow_type = 5 / 2 * 7;
  }

  bool is_black();

  ~Cat() {}
};
